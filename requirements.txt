# Direct dependencies only — transitive packages are resolved by pip.
# Previously this file was a full `pip freeze` dump, which meant every
# transitive pin (urllib3, jinja2, tornado, pillow, ...) became its own
# Dependabot alert. Floors below are the first versions clear of the
# advisories reported in Aug 2026.
# Requires Python 3.11+.

# --- Elastic ---
elasticsearch>=8.19.0,<10
elastic-apm>=6.26.2
ecs-logging>=2.3.0

# --- Config / env ---
python-dotenv>=1.0.1

# --- Streamlit app (streamlit/example-streamlit.py) ---
streamlit>=1.42.0
streamlit-authenticator>=0.4.2

# --- Data / notebooks ---
pandas>=2.2.3
ipykernel>=6.29.5
requests>=2.32.4

# --- Terraform helpers (tools/terraformes.py) ---
python-terraform>=0.10.1
